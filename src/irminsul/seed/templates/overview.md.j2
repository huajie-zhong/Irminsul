---
id: overview
title: Architecture overview
status: draft
describes: []
---

# Architecture overview

C4 Level 1 — {{ project_name }} as a single box, plus the users and external
systems it talks to.

{{ project_name }} exists to deliver this first: {{ idea }}

Its first user is {{ first_user }}.

Container-level (C4 L2) detail belongs in a containers doc of its own once the
system grows past a single box.
