project_name = "soft-change-binding-fixture"

[paths]
docs_root = "docs"
source_roots = ["app"]

[checks]
soft_deterministic = ["change-binding"]

[[checks.inventory_drift.generic]]
kind = "events"
glob = "app/**/events.py"
pattern = 'EVENT\("([^"]+)"\)'
