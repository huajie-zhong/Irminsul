---
id: auth-routing
title: Auth routing
audience: explanation
tier: 3
status: stable
describes:
  - app/auth/routing/*.py
tests:
  - tests/test_edges_routing.py
---

# Auth routing

Owns the auth route table — a narrower claim inside the auth component.
