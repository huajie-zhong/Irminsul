---
id: repeats
title: Repeats
status: stable
---

# Repeats

- `widget build` compiles every widget in the workspace and writes the bundle to the output directory.
- `widget clean` removes the output directory.
- `widget build` compiles every widget in the workspace and writes the bundle to the output directory.

Short line.

Short line.

```text
- `widget build` compiles every widget in the workspace and writes the bundle to the output directory.
```
