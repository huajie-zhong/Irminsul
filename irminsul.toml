project_name = "Irminsul"

[paths]
docs_root = "docs"
source_roots = ["src"]

[checks]
soft_deterministic = ["mtime-drift", "orphans", "stale-reaper"]

[languages]
enabled = ["python"]

[render]
target = "mkdocs"
site_dir = "site"
